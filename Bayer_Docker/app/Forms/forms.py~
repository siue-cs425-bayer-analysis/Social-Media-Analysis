


from django import forms

class SearchForm(forms.Form):
  Choices=['reddit', 'twitter']
  searchTerm = forms.ChaField(label='Search Term', max_length=100)
  selected = forms.Select(label='Select social media site...', choices= Choices)
  
